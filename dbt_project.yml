
name: 'adwords'
version: '1.0'

source-paths: ["models"]
analysis-paths: ["analysis"]
target-path: "target"
clean-targets: ["target"]
test-paths: ["test"]
data-paths: ["data"]

models:
  adwords:
    stitch:
      enabled: true
      materialized: view
      vars:
        adwords_base_table:             #"schema.table"
        campaign_performance_report:    #"schema.table"
        criteria_performance_report:    #"schema.table"
        click_performance_report:       #"schema.table"
